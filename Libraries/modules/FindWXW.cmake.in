# Description:
# This module finds the location of the wxWidgets source and binary paths.
#

# add search path for CURL
SET(wxWidgets_DIR @MAF_WXW_DIR@)
find_package(wxWidgets COMPONENTS base net core adv html aui REQUIRED CONFIG)
